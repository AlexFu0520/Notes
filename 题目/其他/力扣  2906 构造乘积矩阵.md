给你一个下标从 **0** 开始、大小为 `n * m` 的二维整数矩阵 `grid` ，定义一个下标从 **0** 开始、大小为 `n * m` 的的二维矩阵 `p`。如果满足以下条件，则称 `p` 为 `grid` 的 **乘积矩阵** ：

- 对于每个元素 `p[i][j]` ，它的值等于除了 `grid[i][j]` 外所有元素的乘积。乘积对 `12345` 取余数。

返回 `grid` 的乘积矩阵。

**示例 1：**

**输入：** $grid = [[1,2],[3,4]]$
**输出：** $[[24,12],[8,6]]$
**解释：** $p[0][0] = grid[0][1] * grid[1][0] * grid[1][1] = 2 * 3 * 4 = 24$
$p[0][1] = grid[0][0] * grid[1][0] * grid[1][1] = 1 * 3 * 4 = 12$
$p[1][0] = grid[0][0] * grid[0][1] * grid[1][1] = 1 * 2 * 4 = 8$
$p[1][1] = grid[0][0] * grid[0][1] * grid[1][0] = 1 * 2 * 3 = 6$
所以答案是 $[[24,12],[8,6]]$ 。

**示例 2：**

**输入：** $grid = [[12345],[2],[1]]$
**输出：** $[[2],[0],[0]]$
**解释：** $p[0][0] = grid[0][1] * grid[0][2] = 2 * 1 = 2$
$p[0][1] = grid[0][0] * grid[0][2] = 12345 * 1 = 12345. 12345\% 12345 = 0 ，所以 p[0][1] = 0$
$p[0][2] = grid[0][0] * grid[0][1] = 12345 * 2 = 24690. 24690 \% 12345 = 0 ，所以 p[0][2] = 0$
所以答案是 $[[2],[0],[0]]$ 。

**提示：**

- `1 <= n == grid.length <= 105`
- `1 <= m == grid[i].length <= 105`
- `2 <= n * m <= 105`
- `1 <= grid[i][j] <= 109`
## 思路：
1. 先算出从 $grid[i][j]$ 的下一个元素开始，到最后一个元素 $grid[n−1][m−1]$ 的乘积，记作 $suf[i][j]$。这可以从最后一个数 $grid[n−1][m−1]$ 开始，倒着遍历 grid 得到。
2. 然后算出从第一个数 $grid[0][0]$ 开始，到 $grid[i][j]$ 的上一个元素的乘积，记作 $pre[i][j]$ 。这可以从第一行第一列开始，正着遍历得到。
3. 那么:   $$p[i][j]=pre[i][j]⋅suf[i][j]$$
4. 代码实现时，可以先初始化 $p[i][j]=suf[i][j]$，然后在计算 $pre[i][j]$ 的过程中，把 $pre[i][j]$ 乘到 $p[i][j]$ 中，就得到了最终答案。这样写的话，pre 和 suf 可以直接用单个变量表示，无需创建数组。
## ACcode:
### 例如：
1. $\begin{bmatrix} 1&2\\3 &4\end{bmatrix} \frac{计算后缀}{\Rightarrow} \frac{\begin{bmatrix}24&12\\4&1\end{bmatrix}计算前缀}{}$
```cpp
class Solution {
public:
    vector<vector<int>> constructProductMatrix(vector<vector<int>>& grid) {
        int mod = 12345;
        int n =grid.size(),m = grid[0].size();
        vector ans(n,vector<int>(m));

        long long suf = 1;
        for (int i = n - 1 ; i >= 0 ; --i) {
            for (int j = m - 1 ; j >= 0 ; --j) {
		          ans[i][j] = suf;
		          suf = suf * grid[i][j] % mod;
			}
        }
        
        long long pre = 1;
        for (int i = 0 ; i < n ; ++i) {
	        for  (int j = 0 ; j < m ; ++j) {
		        ans[i][j] = ans[i][j] * pre % mod;
		        pre = pre * grid[i][j] % mod;
	        }
        }
        return ans;
    }
};
```
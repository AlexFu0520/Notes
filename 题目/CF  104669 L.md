<div  style="text-align:center; font-weight:bold; font-size:2em; "> L. Turtle and GCD </div>
<center>time limit per test: 2 seconds</center>
<center>memory limit per test: 256 megabytes</center>
<center>input: standard input</center>
<center>output: standard output</center>

*Algorithm :* [[数学算法]]，[[gcd和lcm]]

This problem was supposed to have a cool statement but I forgot to add it.

Given two numbers $a$ and $b$, consider the set of integers $a$, $a + 1$, $a + 2$, …, $a + b - 1$. Partition this set into two nonempty sets such that each element belongs to exactly one set and the GCD of the sum of the sets is maximized. What is that maximum GCD?

**Input**
Each test contains multiple test cases. The first line contains the number of test cases $t$ ($1 \le t \le 100$). The description of the test cases follows.

The first line contains two space separated integers $a$ and $b$ ($1 \le a \le 10^5$, $2 \le b \le 10^5$).

It is guaranteed that the sum of $a$ over all test cases is at most $10^5$.

It is also guaranteed that the sum of $b$ over all test cases is at most $10^5$.

**Output**
For each test case, output a single line, the maximum possible GCD.
**Example**

**Input**
```plaintext
6
11 4
3 3
1 6
25 36
6253 9564
69 420
```

**Output**
```plaintext
25
4
7
765
52766979
58485
```

**Note**

In the first test case, the numbers are $[11, 12, 13, 14]$ which we can partition into $[11, 14]$ and $[12, 13]$. The sum of the sets are $11 + 14 = 25$ and $12 + 13 = 25$ and GCD$(25, 25) = 25$. Guys after reading this, please be sure to orz Richard (both of them).

In the second test case, the set of numbers is $[3, 4, 5]$ which we partition into $[3, 5]$ and $[4]$. The sum of the subsets are $3 + 5 = 8$ and $4$ and GCD$(4, 8) = 4$.
### 思路：
1. 设两个子集的和为 $s_{1}$ 和 $s_{2}$ ， 则：$$s_{1} + s_{2}=total~~~(total为整个集合的总和)$$
2. $$gcd(s_{1},s_{2})=gcd(s_{1},total)~~(GCD的性质：gcd(x,y)=gcd(x,x + y))$$
3. 因此问题转化为：找到 total 的最大约数 d ，使得存在非空真子集的和能被 d 整除
4. 代码的核心逻辑分三步：
	- 计算集合总和total
	- 定义 check 函数: 判断某个数 d (total 的约数)是否可行(即存在子集和能被d整除)
	- 枚举total的所有约数，用check函数验证，找到最大的可行约数
### code :
```cpp
#include <iostream>
#include <vector>
#include <algorithm>
typedef long long ll;
using namespace std;

void solve() {
    ll a,b;
    cin >> a >> b;
    //total 为集合的总和
    ll total = 1ll * (a * 2 + b - 1) * b / 2;
    auto check = [&](ll x) -> bool {
        for (ll i = 1 ; i < b ; ++i) {
	        //l代表该集合中长度为i的最小和 在开头等差求和
	        //r代表该集合中长度为i的最大和 在结尾等差求和
	        //在区间[l,r]中的数都可以取道
            ll l = a * i + i * (i - 1) / 2;
            ll r = a * i + (2 * b - 1 - i) * i / 2;
            // 判断区间[l,r]中是否存在x的倍数
		    //r/x*x 计算 <=r 的最大x的倍数,若这个倍数 >=l,说明区间[l,r]中存在x的倍数
            if (r / x * x >= l) return true;
        }
        return false;
    };

    ll ans = 0;
    for ( ll i = 1 ; i * i <= total ; ++i) {
	    //枚举total的所有约数，找最大可行约数
        if (total % i == 0) {
            if (check(i)) ans = max(ans,i);
            if (check(total / i)) ans = max(ans,total / i);
        }
    }
    cout << ans << '\n';
}               

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int T = 1;
    cin >> T;
    while (T--) {
        solve();   
    }
    return 0;
}
```